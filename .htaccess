<IfModule mod_rewrite.c>
    RewriteEngine On

    # Reescribe las peticiones para que apunten a la carpeta public/
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>